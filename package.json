{
  "name": "dmarc-dashboard-kit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash deploy.sh",
    "migrate": "for f in $(ls migrations/*.sql | sort); do wrangler d1 execute dmarc-reports --file=$f --remote; done",
    "migrate:local": "for f in $(ls migrations/*.sql | sort); do wrangler d1 execute dmarc-reports --file=$f --local; done"
  },
  "dependencies": {
    "fast-xml-parser": "^5.5.7",
    "mime-db": "^1.54.0",
    "pako": "^2.1.0",
    "postal-mime": "^2.7.4",
    "unzipit": "^1.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/mime-db": "^1.43.6",
    "@types/pako": "^2.0.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.76.0"
  }
}
